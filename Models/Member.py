class Member:
    def __init__(self, id, name, discriminator, bot, nick):
        self.id = id
        self.name = name
        self.discriminator = discriminator
        self.bot = bot
        self.nick = nick